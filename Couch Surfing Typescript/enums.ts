// The enums file contains the enumerated types, which are data structures of constant length that hold a set of constant values. They are a set of named constants

// Note the Permissions object that contains two constants, ADMIN and READ_ONLY:
export enum Permissions {
    ADMIN = "ADMIN",
    READ_ONLY = "READ_ONLY"
}

export enum LoyaltyUser {
    GOLD_USER = "GOLD_USER",
    SILVER_USER = "SILVER_USER",
    BRONZE_USER = "BRONZE_USER"
}